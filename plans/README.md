# Implementation Plans

Generated by the improve skill on 2026-06-12, against commit `e8f2e6b`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Note: plans 001 and 002 include manual operator steps (Cloudflare key
rotation, Apps Script redeploy) that an executor agent cannot perform alone.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Rotate leaked Turnstile secret, move to Script Properties | P1 | S | — | DONE (code, on `advisor/001-002-intake-fixes`) — operator steps pending: rotate key, set Script Property, redeploy |
| 002  | Stop showing success for silently dropped intakes | P1 | M | 001 | DONE (code, on `advisor/001-002-intake-fixes`) — operator steps pending: redeploy server BEFORE pushing HTML, live CORS check, test matrix |
| 003  | Cut landing-page image payload to <1 MB | P2 | S–M | — | DONE (on `advisor/003-optimize-images`, which stacks all five plans) — reviewer visual pass done; ~4.77 MB saved |
| 004  | Accept accented/non-ASCII names in signatures | P2 | S | — | DONE (on `advisor/004-005-fixes`, stacked on 001-002) |
| 005  | Truth-up CLAUDE.md; delete dead intake-template.html | P2 | S | — | DONE (on `advisor/004-005-fixes`) |
| 006  | Live staged feedback while the intake form submits | P2 | S | 002 | DONE (merged to master at 501c8b0) |
| 007  | SEO cleanup — robots.txt, sitemap, Psychologist schema, OG gaps | P3 | S | — | DONE (merged to master) |
| 008  | Restore eager dual-hero loading for smooth theme crossfade | P2 | S | 003 | DONE (merged to master) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 002 follows 001 because both edit the same region of `doPost` in
  `Code.gs`, and 002 should be written against the property-based secret
  lookup. Both end with a manual Apps Script redeploy — batching them into
  one redeploy is fine.
- 002 and 004 both edit `new-client-form/index.html` (different sections);
  if run concurrently, expect a trivial merge.
- 005 should land last if possible, so it can document the post-001/002
  state of `Code.gs`.

## Findings considered and rejected / deferred

- **PHI over plain email** (intake PDF with mental-health data, incl. a
  "Suicidal Thoughts" checkbox, sent via GmailApp): this is the practice's
  established workflow; re-architecting delivery is a risk-acceptance
  decision for the practice owner, not an advisor unilateral fix.
- **Tailwind CDN JIT in production**: real perf cost, but the fix
  (precompiled CSS) changes the editing workflow for all future styling
  work. Deferred pending owner decision; revisit as a plan if requested.
- **Shared site.js/site.css extraction** (~600 duplicated lines across 6
  pages, already drifting): worth doing eventually; deferred as MED-risk
  multi-page refactor.
- **"All 32 concerns" intake bug**: diagnostics were added and removed
  (commits 9756f52, e8f2e6b) with no fix in between; cause never identified.
  LOW confidence — investigate-only candidate, not planned.
- **Durable Google Sheet submission log** (direction): highest-value
  follow-up to 002 — one `appendRow` before the spam gates would make data
  loss structurally impossible. Not planned pending owner selection.
- **Client confirmation email** (direction): depends on 002's readable
  success signal. Not planned pending owner selection.
- **robots.txt missing / Turnstile widget hardcoded `data-theme="light"` /
  untracked stitch_ folder**: minor; the last two are noted inside plans
  003/005 where adjacent.
